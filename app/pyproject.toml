[project]
name = "jev-research-desk"
version = "0.1.0"
description = "A macro research desk built from TypeSafe System One judgments"
requires-python = ">=3.12"
dependencies = [
    "typesafe-sdk>=0.5.7",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "yfinance>=1.7.0",
]

[[tool.uv.index]]
name = "typesafe"
url = "https://pypi.typesafe.ai/"

[tool.uv]
package = false
